name: homigo_sdk
description: "Reusable Flutter foundation, design system, platform services, and production infrastructure for HomiGo applications."
version: 1.1.0
homepage: https://github.com/traikeyy-cmd/homigo-sdk
repository: https://github.com/traikeyy-cmd/homigo-sdk
issue_tracker: https://github.com/traikeyy-cmd/homigo-sdk/issues

topics:
  - flutter
  - design-system
  - ui-components
  - networking
  - application-framework

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  connectivity_plus: ^7.3.1
  device_info_plus: ^13.2.0
  file_picker: ^12.0.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  http: ^1.6.0
  image_picker: ^1.2.3
  package_info_plus: ^10.2.1
  permission_handler: ^12.0.3
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
